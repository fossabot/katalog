blueprint:
  security:
    simple:
      enabled: true
      users:
        user:
          username: user
          password: user
          roles:
          - USER
          groups:
          - users
        admin:
          username: admin
          password: admin
          roles:
          - ADMIN
          groups:
          - users
          - administrators

spring:
  main:
    banner-mode: "off"

The Azure plugin requires these permissions:
* `User.Read` to perform the OAuth2 login of users
* `User.Read.All` to get information about all users
* `Group.Read.All` to get information about all groups
spring:
  event-store:
    type: postgres
  blob-store:
    type: postgres
  message-bus:
    type: postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/katalog
    username: katalog
    password: katalog
    driver-class-name: org.postgresql.Driver
